---
title: Skeleton
description: Loading placeholder components that mimic content shapes with a pulse animation.
icon: Loader
---

import {
	Skeleton,
	SkeletonText,
	SkeletonCircle,
	SkeletonRect,
} from "@work-rjkashyap/unified-ui";
import { Callout } from "fumadocs-ui/components/callout";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";

## Basic

Loading placeholders that mimic the shape of content before it loads. Supports text lines, circles, and rectangles with a gentle pulse animation.

<PreviewCard
  preview={
    <div className="flex flex-col gap-6 w-full max-w-sm">
      <div className="flex items-center gap-3">
        <SkeletonCircle size="md" />
        <div className="flex flex-col gap-2 flex-1">
          <SkeletonText lines={1} className="w-3/4" />
          <SkeletonText lines={1} className="w-1/2" size="sm" />
        </div>
      </div>
      <SkeletonRect width="100%" height={120} />
      <SkeletonText lines={3} />
    </div>
  }
  code={`<div className="flex items-center gap-3">
  <SkeletonCircle size="md" />
  <div className="flex flex-col gap-2 flex-1">
    <SkeletonText lines={1} className="w-3/4" />
    <SkeletonText lines={1} className="w-1/2" size="sm" />
  </div>
</div>
<SkeletonRect width="100%" height={120} />
<SkeletonText lines={3} />`}

/>

## Installation

Install the component via the CLI in one command.

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
<Tab value="npm">

```bash
npx @work-rjkashyap/unified-ui add skeleton
```

</Tab>
<Tab value="pnpm">

```bash
pnpm dlx @work-rjkashyap/unified-ui add skeleton
```

</Tab>
<Tab value="yarn">

```bash
npx @work-rjkashyap/unified-ui add skeleton
```

</Tab>
<Tab value="bun">

```bash
bunx @work-rjkashyap/unified-ui add skeleton
```

</Tab>
</Tabs>

<Callout type="info">
	If you haven't initialized your project yet, run `npx @work-rjkashyap/unified-ui init` first. See the [CLI docs](/docs/cli) for details.
</Callout>

## Or install the full package

Use this approach if you prefer to install the entire design system as a dependency instead of copying individual components.

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
<Tab value="npm">

```bash
npm install @work-rjkashyap/unified-ui
```

</Tab>
<Tab value="pnpm">

```bash
pnpm add @work-rjkashyap/unified-ui
```

</Tab>
<Tab value="yarn">

```bash
yarn add @work-rjkashyap/unified-ui
```

</Tab>
<Tab value="bun">

```bash
bun add @work-rjkashyap/unified-ui
```

</Tab>
</Tabs>

## Anatomy

```tsx
import {
	Skeleton,
	SkeletonText,
	SkeletonCircle,
	SkeletonRect,
} from "@work-rjkashyap/unified-ui";
```

## Basic Skeleton

The base `Skeleton` component renders a single animated placeholder. Use the `shape` prop to control its form.

```tsx
<Skeleton shape="text" />
<Skeleton shape="circle" size="md" />
<Skeleton shape="rect" width={200} height={120} />
```

## SkeletonText

Renders one or more text-line placeholders. Use the `lines` prop to control the number of lines. The last line is automatically shorter for a natural appearance.

<PreviewCard
	preview={
		<div className="flex flex-col gap-6 max-w-sm w-full">
			<div>
				<p className="text-xs text-muted-foreground mb-2">1 line</p>
				<SkeletonText lines={1} />
			</div>
			<div>
				<p className="text-xs text-muted-foreground mb-2">3 lines</p>
				<SkeletonText lines={3} />
			</div>
			<div>
				<p className="text-xs text-muted-foreground mb-2">5 lines</p>
				<SkeletonText lines={5} />
			</div>
		</div>
	}
	code={`<SkeletonText lines={1} />
<SkeletonText lines={3} />
<SkeletonText lines={5} />`}
/>

### Text Sizes

Two sizes control the height of each text line to match your typography scale.

<PreviewCard
	preview={
		<div className="flex flex-col gap-4 max-w-sm w-full">
			<div>
				<p className="text-xs text-muted-foreground mb-2">Small</p>
				<SkeletonText lines={2} size="sm" />
			</div>
			<div>
				<p className="text-xs text-muted-foreground mb-2">
					Medium (default)
				</p>
				<SkeletonText lines={2} size="md" />
			</div>
		</div>
	}
	code={`<SkeletonText lines={2} size="sm" />
<SkeletonText lines={2} size="md" />`}
/>

## SkeletonCircle

Renders a circular placeholder. Perfect for avatar and icon loading states.

<PreviewCard
	preview={
		<div className="flex gap-4 items-end justify-center">
			<SkeletonCircle size="xs" />
			<SkeletonCircle size="sm" />
			<SkeletonCircle size="md" />
			<SkeletonCircle size="lg" />
			<SkeletonCircle size="xl" />
		</div>
	}
	code={`<SkeletonCircle size="xs" />  {/* 24px */}
<SkeletonCircle size="sm" />  {/* 32px */}
<SkeletonCircle size="md" />  {/* 40px — default */}
<SkeletonCircle size="lg" />  {/* 48px */}
<SkeletonCircle size="xl" />  {/* 64px */}`}
/>

| Size | Dimensions | Matches   |
| ---- | ---------- | --------- |
| `xs` | 24×24px    | Avatar xs |
| `sm` | 32×32px    | Avatar sm |
| `md` | 40×40px    | Avatar md |
| `lg` | 48×48px    | Avatar lg |
| `xl` | 64×64px    | Avatar xl |

## SkeletonRect

Renders a rectangular placeholder with configurable width and height. Useful for images, cards, and media content loading states.

<PreviewCard
	preview={
		<div className="flex flex-col gap-4 max-w-sm w-full">
			<SkeletonRect width="100%" height={80} />
			<SkeletonRect width={200} height={120} />
			<SkeletonRect width="50%" height={60} />
		</div>
	}
	code={`<SkeletonRect width="100%" height={80} />
<SkeletonRect width={200} height={120} />
<SkeletonRect width="50%" height={60} />`}
/>

## Composition Examples

### User Card Skeleton

A common loading pattern for user profile cards.

<PreviewCard
	preview={
		<div className="max-w-sm w-full border border-border rounded-md p-4">
			<div className="flex items-center gap-3 mb-4">
				<SkeletonCircle size="lg" />
				<div className="flex flex-col gap-2 flex-1">
					<SkeletonText lines={1} className="w-2/3" />
					<SkeletonText lines={1} className="w-1/3" size="sm" />
				</div>
			</div>
			<SkeletonText lines={3} />
		</div>
	}
	code={`<Card>
  <CardBody>
    <div className="flex items-center gap-3 mb-4">
      <SkeletonCircle size="lg" />
      <div className="flex flex-col gap-2 flex-1">
        <SkeletonText lines={1} className="w-2/3" />
        <SkeletonText lines={1} className="w-1/3" size="sm" />
      </div>
    </div>
    <SkeletonText lines={3} />
  </CardBody>
</Card>`}
/>

### Article Skeleton

A loading placeholder for article or blog post content.

<PreviewCard
	preview={
		<div className="max-w-sm w-full flex flex-col gap-4">
			<SkeletonRect width="100%" height={160} />
			<SkeletonText lines={1} className="w-3/4" size="md" />
			<SkeletonText lines={1} className="w-1/2" size="sm" />
			<SkeletonText lines={4} />
		</div>
	}
	code={`<SkeletonRect width="100%" height={160} />
<SkeletonText lines={1} className="w-3/4" size="md" />
<SkeletonText lines={1} className="w-1/2" size="sm" />
<SkeletonText lines={4} />`}
/>

### Table Row Skeleton

Loading state for data table rows.

<PreviewCard
	preview={
		<div className="max-w-md w-full flex flex-col gap-3">
			{[1, 2, 3].map((i) => (
				<div key={i} className="flex items-center gap-3">
					<SkeletonCircle size="sm" />
					<SkeletonText lines={1} className="flex-1" />
					<Skeleton shape="text" className="w-16" />
					<Skeleton shape="text" className="w-20" />
				</div>
			))}
		</div>
	}
	code={`{rows.map((_, i) => (
  <div key={i} className="flex items-center gap-3">
    <SkeletonCircle size="sm" />
    <SkeletonText lines={1} className="flex-1" />
    <Skeleton shape="text" className="w-16" />
    <Skeleton shape="text" className="w-20" />
  </div>
))}`}
/>

## Custom Dimensions

Both `width` and `height` accept numbers (pixels) or strings (CSS values like percentages or `auto`).

```tsx
// Pixel values
<SkeletonRect width={300} height={200} />

// CSS string values
<SkeletonRect width="100%" height={120} />
<SkeletonRect width="50%" height="8rem" />

// Full width text
<SkeletonText lines={2} className="w-full" />
```

## Props

### Skeleton

| Prop        | Type                                   | Default  | Description                                   |
| ----------- | -------------------------------------- | -------- | --------------------------------------------- |
| `shape`     | `"text" \| "circle" \| "rect"`         | `"text"` | Shape of the skeleton placeholder.            |
| `size`      | `"xs" \| "sm" \| "md" \| "lg" \| "xl"` | `"md"`   | Size preset (used with `circle` shape).       |
| `width`     | `number \| string`                     | —        | Custom width. Numbers are treated as pixels.  |
| `height`    | `number \| string`                     | —        | Custom height. Numbers are treated as pixels. |
| `className` | `string`                               | —        | Additional CSS classes.                       |

### SkeletonText

| Prop        | Type           | Default | Description                     |
| ----------- | -------------- | ------- | ------------------------------- |
| `lines`     | `number`       | `3`     | Number of text lines to render. |
| `size`      | `"sm" \| "md"` | `"md"`  | Height of each text line.       |
| `className` | `string`       | —       | Additional CSS classes.         |

### SkeletonCircle

| Prop        | Type                                   | Default | Description             |
| ----------- | -------------------------------------- | ------- | ----------------------- |
| `size`      | `"xs" \| "sm" \| "md" \| "lg" \| "xl"` | `"md"`  | Diameter of the circle. |
| `className` | `string`                               | —       | Additional CSS classes. |

### SkeletonRect

| Prop        | Type               | Default  | Description              |
| ----------- | ------------------ | -------- | ------------------------ |
| `width`     | `number \| string` | `"100%"` | Width of the rectangle.  |
| `height`    | `number \| string` | `80`     | Height of the rectangle. |
| `className` | `string`           | —        | Additional CSS classes.  |

## Accessibility

<Callout type="info">
	Skeleton placeholders are purely decorative and should not convey meaningful
	information.
</Callout>

- All skeleton elements render with `aria-hidden="true"` so screen readers skip them.
- The root element uses `role="presentation"` to indicate decorative content.
- The pulse animation automatically respects `prefers-reduced-motion` — when the user prefers reduced motion, the animation is disabled via the global CSS rule in `styles.css`.
- When using skeletons as loading states, ensure the surrounding container or page communicates the loading state to assistive technology (e.g., via `aria-busy="true"` on the parent, or a visually hidden "Loading..." text).

## Reduced Motion

The skeleton pulse animation is defined via `animate-pulse` in Tailwind, which is automatically disabled when the user has `prefers-reduced-motion: reduce` enabled. No additional configuration is needed.

```css
/* Handled globally in styles.css */
@media (prefers-reduced-motion: reduce) {
	.animate-pulse {
		animation: none;
	}
}
```

## Design Tokens

| Token              | Usage                            |
| ------------------ | -------------------------------- |
| `--muted` | Skeleton placeholder background  |
| `--radius-sm`   | Text line and rect border radius |
| `--radius-full` | Circle shape border radius       |
